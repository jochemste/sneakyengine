#ifndef PROCESS_HPP
#define PROCESS_HPP

/// Temporary C style function. Will be replaced by class
/// @return 0 when success, -1 when fail
int PROC_init();

/// Temporary C style function. Will be replaced by class
void PROC_quit();

#endif // PROCESS_HPP
